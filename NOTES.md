# 작업 인수인계 노트

다른 컴퓨터나 새 세션에서 이어서 작업할 때 이 문서부터 읽으면 됩니다.
서비스 사용법·구조 설명은 [README.md](README.md)에 있고, 여기에는 **지금 상태와 남은 일**만 적습니다.

마지막 갱신: 2026-08-18

---

## 1. 지금 어떤 상태인가

| 항목 | 상태 |
|---|---|
| 프로덕션 | https://onair-three-beta.vercel.app (정상) |
| Vercel 프로젝트 | `onair` (프로젝트 ID는 로컬 `.vercel/project.json` 또는 Vercel 대시보드에서) |
| Supabase 프로젝트 | `ushs-on-air` (서울 리전 / 프로젝트 ref는 Supabase 대시보드에서) |
| 중복 방지 마이그레이션 | ✅ 적용 완료 (unique index 2개 확인) |
| 학생 신청 | 0건 (아직 공지 전) |
| 검색 캐시 | iTunes 209건 / Deezer 124건 예열됨 |

### 새 컴퓨터에서 시작하기

```bash
gh repo clone onair && cd onair && npm install && npx vercel link
```

로컬에서 돌리려면 `.env.local`이 필요한데, **Vercel에서 값을 내려받을 수 없습니다** (아래 6번 참고).

---

## 2. 아직 안 끝난 일

### 2-1. 결과 안내 메일이 꺼져 있음 ⚠️

코드는 다 들어가 있는데 환경변수가 없어서 **메일만 조용히 비활성** 상태입니다. 나머지 기능은 정상 동작합니다.

필요한 값: `RESEND_API_KEY`, `MAIL_FROM` (`APP_URL`은 코드에 기본값이 있어 생략 가능)

`MAIL_FROM`이 미해결 지점입니다. Resend는 **DNS 인증을 마친 본인 소유 도메인**만 발신자로 허용합니다.
- `vercel.app` 주소는 **불가** — DNS가 Vercel 소유, Public Suffix List 등재, MX 레코드 없음 (확인함)
- 선택지: ① 도메인 구입(연 1~2만원) ② 학교 도메인 `ushs.hs.kr`에 학교 IT가 레코드 추가 ③ Gmail SMTP로 전환(도메인 불필요, 하루 500통, `nodemailer` 필요 — 코드 변경 필요)
- 학교 메일은 Google Workspace 기반이라(MX가 `ASPMX.L.GOOGLE.COM`) **Gmail SMTP 방식이 스팸 회피에 가장 유리**합니다

Resend 무료 티어는 하루 100통/월 3,000통이라, 신청 접수 시점에는 안 보내고 **결과 확정 시(승인·반려·방송일 배정)만** 발송하도록 설계돼 있습니다.

### 2-2. 비속어 사전이 비어 있음 ⚠️

`blocklist_keywords` 테이블 **0건**. 자동 필터 3중 방어선 중 하나가 사실상 꺼진 상태입니다.
국내 스토어는 explicit 마킹이 잘 안 붙어서 이 사전이 실질적 보완책인데 지금은 아무 역할을 못 합니다.
`/admin` → 비속어 사전 탭에서 추가하면 됩니다.

### 2-3. 검색으로 못 잡는 곡 (코드로 해결 불가)

`롤러코스터`(칠린호미), `Ghost`(저스디스) 같은 흔한 제목은 200위까지 봐도 유명 아티스트에 묻힙니다.
아예 iTunes KR 카탈로그에 없는 곡도 있습니다(빈지노 `Aqua Man` — Deezer에만 존재).
→ 아티스트명 검색 또는 "곡명으로 직접 신청하기"로 우회. 헤더 안내 문구가 이 근거로 유지되고 있습니다.

---

## 3. 검색 알고리즘 — 이미 조사한 것 (재조사 불필요)

국내 힙합 15곡을 iTunes/Deezer에 직접 호출(71회)해 확인한 결과입니다.

**원인은 4가지였습니다:**
1. **노래방·MR 클론이 상위를 점령** — `아무노래` 상위 10개 중 6개가 클론, `Aqua Man`은 1위가 금영노래방
2. **병합 순서** — 예전에는 iTunes 전부 → Deezer 전부 이어붙이기라, Deezer에만 있는 국내 힙합이 항상 최하단으로 밀림
3. **iTunes KR 카탈로그 누락**
4. **흔한 제목이 인기순에 묻힘**

**적용한 해결책** (`lib/search.js`):
- 클론 필터 (아티스트명 우선 판단, 제목은 명확한 표기만 — 오탐이 더 나쁘므로 보수적)
- **iTunes 2 : Deezer 1** 비율로 섞기. 1:1은 시도했다가 되돌렸음 — `newjeans` 2위에 League of Legends가 뜨는 등 Deezer의 한국어 관련도가 나빠서 상단이 오염됨
- 소스별 `limit=50` (호출 횟수는 limit과 무관하게 1회라 레이트 리밋 영향 없음), 최종 40곡 반환, 화면은 10개씩 더보기

**시도했다가 버린 것:** 관련도 재정렬 → 동명 제목이 많아 변별력이 없고 오히려 악화됨 (빈지노 `Aqua Man`이 12위 → 사라짐)

---

## 4. 부하 특성 — 이미 분석한 것

- **앱 계층은 병목이 아님**: 200 동시 연결에서 4,025 req/s, p50 48ms (로컬 프로덕션 빌드 실측)
- **유일한 병목은 iTunes 분당 18회**
- **중요**: 한도 초과 시 대기시키지 않고 즉시 Deezer 결과만 반환하는 구조 → **렉이 아니라 "국내곡 검색 품질 저하"로 나타남**
- Deezer는 국내곡 커버리지가 얇음 (`iu` 0건, `밖에 비와` 1건)
- 캐시 12시간 TTL이 러시 완화의 핵심. 인기곡은 검색어가 겹쳐서 실제 호출이 크게 줄어듦

장애 시나리오는 fault injection으로 전부 검증 완료 — 한쪽 API가 죽거나 429거나 무응답이어도 200으로 응답하고 다른 소스 결과를 보여줍니다. 무응답은 5초에 끊깁니다.

---

## 5. 감시 방법

모니터는 **로컬 프로세스**라 컴퓨터를 옮기면 따라가지 않습니다. 새로 띄워야 합니다.

가장 신뢰할 수 있는 지표는 **DB 직접 조회**입니다 (Supabase MCP 사용):

```sql
-- 프로젝트 ref는 Supabase 대시보드에서 확인
select
  (select count(*) from song_requests) as 신청누적,
  (select count(distinct student_id) from song_requests) as 학생수,
  (select count(*) from itunes_call_log where called_at > now() - interval '1 minute') as itunes_최근1분,
  (select count(*) from blocklist_keywords) as 사전키워드;
```

레이트 리밋이 걸리면 서버 로그에 `[RATE_LIMIT]` 경고가 남습니다 (`lib/itunes.js`, `lib/deezer.js`).
이게 뜨면 검색 품질이 떨어지기 시작했다는 뜻입니다. 대응 옵션: 캐시 TTL 연장, `RATE_LIMIT_PER_MINUTE` 상향(Vercel은 인스턴스마다 IP가 달라 실제 여유가 더 있음).

---

## 5-1. 저장소가 공개라는 점 (보안)

이 저장소는 공개되어 있습니다. 즉 **방송부 페이지 경로(`/admin`)와 인증 방식이 누구에게나 보입니다.**
인증은 공유 비밀번호 하나(`x-admin-password` 헤더)뿐이고, **시도 횟수 제한이 없습니다.**

- `ADMIN_PASSWORD`는 추측하기 어려운 값이어야 합니다. 짧거나 규칙적인 값(`onair2026` 류)은 위험합니다.
- 비밀번호가 뚫리면 신청 데이터 전체 삭제까지 가능합니다(데이터 관리 탭).
- 개선하려면: 인증 실패 시 지연/횟수 제한 추가, 또는 방송부 계정을 Supabase Auth로 전환.

## 6. 함정 (모르면 시간 날림)

- **`vercel env pull`로 비밀값을 못 받습니다.** Supabase 키와 `ADMIN_PASSWORD`가 Sensitive로 설정돼 있어 빈 값(`""`)으로 내려옵니다. 보안상 올바른 설정이지만, 로컬에서 실 DB로 테스트하려면 Supabase 대시보드에서 직접 복사해야 합니다.
- **`vercel logs`는 스트리밍이 아닙니다.** 접속할 때마다 최근 과거 로그를 다시 쏟아냅니다. 그대로 집계하면 트래픽이 몇 배로 부풀려집니다. 반드시 로그 id로 중복 제거하고, 감시 시작 시각 이전 항목은 버려야 합니다. (이걸로 두 번 헛짚었음)
- **Next dev 서버가 CSS를 404 낼 때가 있습니다.** 페이지가 통째로 흰색으로 뜨면 CSS 문제가 아니라 dev 서버 캐시 문제입니다. `rm -rf .next` 후 재시작하면 됩니다. 프로덕션 빌드는 정상입니다.
- **하루 신청 한도는 KST 자정 기준**입니다. Vercel은 UTC로 돌기 때문에 서버 로컬 시간에 의존하면 안 됩니다 (`lib/dailyQuota.js`에서 오프셋 직접 계산, 서버 TZ 무관 검증 완료).

---

## 7. 아직 안 만든 기능 (제안했던 것 중)

- 인기 신청곡 랭킹 (DB 변경 없이 group by만으로 가능, 비용 대비 효과 큼)
- 대시보드 일괄 승인 (체크박스 다중 선택)
- 배정 불가 날짜(시험기간·방학) 설정
- CSV 내보내기
- 학생 본인 신청 취소
